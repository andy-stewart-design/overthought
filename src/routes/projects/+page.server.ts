// import projectData from "@content/projects.json";
// import type { PageServerLoad } from "./$types";

// export const load: PageServerLoad = () => {
// 	const projects = projectData.entries;
// 	const featured = projects.filter((p) => p.metadata.featured).map((p) => p.metadata);
// 	const rest = projects.filter((p) => !p.metadata.featured).map((p) => p.metadata);

// 	return {
// 		projects: {
// 			featured,
// 			rest,
// 		},
// 	};
// };
